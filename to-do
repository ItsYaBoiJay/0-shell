Done functions:
echo ✓
pwd ✓ 
exit ✓
cat ✓
ls (flags) -l, -a, -F ✓
must manage the program interuption Ctrl + D ✓
cd ✓

Still unsure:
mv 
cp 
rm (flags) (can't rm folders, if they aren't empty)
mkdir (flags) -p (make sure that you can use parent/child 
to add folder and sub-folders)

Bonus: 
Manage the interruption Ctrl + C
Auto complete when you are writing
Add piping
Add redirection
Have your path behind the $ like (~/Desktop/0-shell $)
Add colors for the directories or errors
Other advanced commands you may like